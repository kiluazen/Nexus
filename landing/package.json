{
  "name": "nexus-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8789",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.84.1"
  }
}
